<UserControl x:Class="tmkbCompanion.MVVM.View.PetCompanionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Height="110" Width="130"
             IsHitTestVisible="True"
             d:DesignHeight="110" d:DesignWidth="130">
    <Grid>
        <!-- Custom visual representation of Bongo Cat -->
        <Canvas Width="120" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center">
            
            <!-- Ears -->
            <!-- Left Ear -->
            <Path Data="M 25,40 L 15,15 L 45,30 Z" Fill="#FFB067" Stroke="#2a3142" StrokeThickness="2"/>
            <Path Data="M 27,38 L 20,20 L 40,30 Z" Fill="#FFA0B0"/> <!-- Inner left ear -->
            
            <!-- Right Ear -->
            <Path Data="M 95,40 L 105,15 L 75,30 Z" Fill="#FFB067" Stroke="#2a3142" StrokeThickness="2"/>
            <Path Data="M 93,38 L 100,20 L 80,30 Z" Fill="#FFA0B0"/> <!-- Inner right ear -->
            
            <!-- Cat Head/Body -->
            <Border Canvas.Left="20" Canvas.Top="25" Width="80" Height="60" CornerRadius="30" Background="#FFB067" BorderBrush="#2a3142" BorderThickness="2"/>
            
            <!-- Eyes -->
            <!-- Normal Eyes -->
            <Ellipse x:Name="LeftEyeNormal" Canvas.Left="42" Canvas.Top="45" Width="6" Height="6" Fill="#2a3142">
                <Ellipse.RenderTransform>
                    <TranslateTransform x:Name="LeftEyeTransform"/>
                </Ellipse.RenderTransform>
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsClickedReactionActive, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <Ellipse x:Name="RightEyeNormal" Canvas.Left="72" Canvas.Top="45" Width="6" Height="6" Fill="#2a3142">
                <Ellipse.RenderTransform>
                    <TranslateTransform x:Name="RightEyeTransform"/>
                </Ellipse.RenderTransform>
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsClickedReactionActive, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            
            <!-- Happy Eyes -->
            <Path x:Name="LeftEyeHappy" Data="M 40,48 Q 45,42 49,48" Stroke="#2a3142" StrokeThickness="2" Fill="Transparent">
                <Path.Style>
                    <Style TargetType="Path">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsClickedReactionActive, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Path.Style>
            </Path>
            <Path x:Name="RightEyeHappy" Data="M 70,48 Q 75,42 79,48" Stroke="#2a3142" StrokeThickness="2" Fill="Transparent">
                <Path.Style>
                    <Style TargetType="Path">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsClickedReactionActive, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Path.Style>
            </Path>
            
            <!-- Blush Cheeks -->
            <Ellipse Canvas.Left="34" Canvas.Top="50" Width="8" Height="4" Fill="#FFA0B0">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Opacity" Value="0.6"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsClickedReactionActive, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="Opacity" Value="1.0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <Ellipse Canvas.Left="78" Canvas.Top="50" Width="8" Height="4" Fill="#FFA0B0">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Opacity" Value="0.6"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsClickedReactionActive, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="Opacity" Value="1.0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>

            <!-- Floating Heart Icon -->
            <Path x:Name="HeartIcon" Data="M 12,5 C 8,0 2,2 2,7 C 2,12 12,17 12,17 C 12,17 22,12 22,7 C 22,2 16,0 12,5 Z" 
                  Fill="#ff6584" Canvas.Left="50" Canvas.Top="10" Width="20" Height="20" Stretch="Uniform" Opacity="0">
                <Path.RenderTransform>
                    <TranslateTransform x:Name="HeartTranslate" Y="0"/>
                </Path.RenderTransform>
                <Path.Style>
                    <Style TargetType="Path">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsClickedReactionActive, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" BeginTime="0:0:0.6" Duration="0:0:0.4"/>
                                            <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)" From="0" To="-25" Duration="0:0:1.0"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Path.Style>
            </Path>
            
            <!-- Snout / Mouth -->
            <Path Data="M 57,51 Q 60,54 63,51 Q 66,54 69,51" Stroke="#2a3142" StrokeThickness="1.5" Fill="Transparent"/>
            <Path Data="M 60,49 L 63,51 L 60,51 Z" Fill="#2a3142"/>
            
            <!-- Desk / Keyboard Base -->
            <Border Canvas.Left="5" Canvas.Top="75" Width="110" Height="22" CornerRadius="4" Background="#1c2230" BorderBrush="#2a3142" BorderThickness="2">
                <!-- Two buttons inside the desk -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Left Button -->
                    <Ellipse x:Name="LeftBtnEllipse" Grid.Column="0" Width="16" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#3a4050"/>
                                <Setter Property="Stroke" Value="#5a6070"/>
                                <Setter Property="StrokeThickness" Value="1"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLeftPawPressed, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                        <Setter Property="Fill" Value="{DynamicResource AccentBrush}"/>
                                        <Setter Property="Stroke" Value="#ffffff"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>

                    <!-- Right Button -->
                    <Ellipse x:Name="RightBtnEllipse" Grid.Column="1" Width="16" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#3a4050"/>
                                <Setter Property="Stroke" Value="#5a6070"/>
                                <Setter Property="StrokeThickness" Value="1"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRightPawPressed, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                        <Setter Property="Fill" Value="{DynamicResource AccentBrush}"/>
                                        <Setter Property="Stroke" Value="#ffffff"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </Grid>
            </Border>

            <!-- Paws -->
            <!-- Left Paw -->
            <Canvas Canvas.Left="28" Canvas.Top="60" Width="22" Height="22">
                <Canvas.Style>
                    <Style TargetType="Canvas">
                        <Setter Property="RenderTransform">
                            <Setter.Value>
                                <TranslateTransform Y="0"/>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLeftPawPressed, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="7"/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Canvas.Style>
                <Ellipse Width="22" Height="16" Fill="#FFC890" Stroke="#2a3142" StrokeThickness="2"/>
                <Ellipse Width="18" Height="12" Fill="#FFFFFF" Canvas.Left="2" Canvas.Top="2"/>
            </Canvas>

            <!-- Right Paw -->
            <Canvas Canvas.Left="70" Canvas.Top="60" Width="22" Height="22">
                <Canvas.Style>
                    <Style TargetType="Canvas">
                        <Setter Property="RenderTransform">
                            <Setter.Value>
                                <TranslateTransform Y="0"/>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRightPawPressed, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="7"/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Canvas.Style>
                <Ellipse Width="22" Height="16" Fill="#FFC890" Stroke="#2a3142" StrokeThickness="2"/>
                <Ellipse Width="18" Height="12" Fill="#FFFFFF" Canvas.Left="2" Canvas.Top="2"/>
            </Canvas>
            
        </Canvas>
    </Grid>
</UserControl>
